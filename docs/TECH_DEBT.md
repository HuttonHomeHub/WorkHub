# Technical debt register

Known shortcuts, gaps, and deferred decisions, with the intent to address them.
Keep this honest and current — undocumented debt is the expensive kind.

> Format: each item has a short description, why it exists, the risk if left,
> and a remediation intent. Remove items when resolved (note it in
> [DECISIONS.md](DECISIONS.md) or `CHANGELOG.md` if significant). Don't cite
> items by number from other docs — numbers change as items are resolved.

| #   | Item                                             | Why it exists                                                                                                                                                                                                                                                             | Risk                                                                                                                                                                                                   | Remediation intent                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **CI e2e runs chromium only, at one viewport**   | To keep CI fast with one browser install, only the chromium project runs there, at Playwright's default Desktop Chrome viewport. The local config also defines webkit, which WorkHub does not support.                                                                    | Firefox-specific and 1280px-layout regressions surface late; the FRONTEND_QUALITY.md test matrix (Chromium + Firefox at 1280×800 and 1920×1080, keyboard-only and 400%-zoom journeys) is not enforced. | Add a Firefox project and explicit 1280×800 / 1920×1080 viewports in CI, drop webkit (see `apps/web/playwright.config.ts`, BACKLOG.md).                                                                                                                        |
| 2   | **Swagger CLI plugin disabled**                  | The `@nestjs/swagger` CLI plugin generated a `metadata.ts` that tripped `noUnusedLocals`. OpenAPI comes from explicit `@Api*` decorators plus the envelope decorators (ADR-0017).                                                                                         | DTO schemas must be annotated by hand.                                                                                                                                                                 | Optionally re-enable `plugins: ["@nestjs/swagger"]` in `nest-cli.json`; verify the build and the contract drift check stay green.                                                                                                                              |
| 3   | **Error responses not in the OpenAPI spec**      | ADR-0017 documents success envelopes; error envelopes per status are not yet described per endpoint.                                                                                                                                                                      | Client code can't get status-specific error types from the contract.                                                                                                                                   | Add an error-envelope decorator (e.g. 401/404/409/422) when client code needs it.                                                                                                                                                                              |
| 4   | **Client errors reach only the browser console** | The web has no client error reporting to the API and no route-level `errorComponent`s — only `RootErrorBoundary`, which logs to the console. (OpenTelemetry is deferred by design, ADR-0019, and is not debt.)                                                            | Client errors are invisible to the owner, and a failing route blanks the app.                                                                                                                          | Post client errors to the API log and add the router's `defaultErrorComponent` (FRONTEND_QUALITY.md, BACKLOG.md).                                                                                                                                              |
| 5   | **No business/domain code yet**                  | Deliberate — the schema holds only the auth tables and the reference feature is a **non-shipping template** (ADR-0014), verified in CI through the generator (`scripts/verify-template.sh`).                                                                              | Docs/template describe intent; real domain still to come.                                                                                                                                              | Generate features with `pnpm gen:feature`; retire the template once real features make it redundant (`docs/REFERENCE_FEATURE.md` → Delete-when-done).                                                                                                          |
| 6   | **Performance budgets are estimates**            | The single-user budgets in `docs/PERFORMANCE.md` were set before a domain or real data existed.                                                                                                                                                                           | Budgets may be too loose or too tight for the real workload.                                                                                                                                           | Re-baseline against measured server times once the first domain feature holds real data.                                                                                                                                                                       |
| 7   | **CSP not finalised for web**                    | App origins/assets unknown until features land.                                                                                                                                                                                                                           | Weaker XSS mitigation than achievable.                                                                                                                                                                 | Tighten the nginx/`helmet` CSP once origins are known.                                                                                                                                                                                                         |
| 8   | **Auth library relatively young**                | Better Auth chosen for fit (ADR-0003) and kept behind the `AuthContextService` seam. Its rate-limit counters are in memory — correct for WorkHub's single API instance (ADR-0019), an assumption to revisit only if that changes.                                         | Ecosystem maturity risk.                                                                                                                                                                               | Monitor releases and advisories; take security updates promptly.                                                                                                                                                                                               |
| 9   | **ESLint pinned to v9**                          | ESLint 10 is available, but `eslint-plugin-import`, `eslint-plugin-jsx-a11y`, and `eslint-plugin-react` still cap their peer range at ESLint 9. Dependabot's major bump is ignored (see `.github/dependabot.yml`).                                                        | Missing ESLint 10 features/fixes until the plugins catch up.                                                                                                                                           | Remove the `eslint` major-ignore and bump the ESLint group once the plugins publish v10-compatible releases.                                                                                                                                                   |
| 10  | **Prisma pinned to v6**                          | Prisma 7 removes `url` from the datasource block and requires a driver adapter + `prisma.config.ts` — a deliberate migration, not a routine bump. The major is ignored in Dependabot.                                                                                     | Missing Prisma 7 improvements until migrated.                                                                                                                                                          | Do the Prisma 7 migration deliberately (driver adapter, `prisma.config.ts`, `PrismaService` wiring) — worth an ADR — then un-ignore.                                                                                                                           |
| 11  | **TypeScript pinned to v5**                      | TypeScript 7 (the native compiler) removed `baseUrl` and `moduleResolution: node10` from tsconfig, which the shared presets rely on for the `@/` and `@repo/*` path aliases. The major is ignored in Dependabot.                                                          | Missing TypeScript 7 speed/features until migrated.                                                                                                                                                    | Migrate the tsconfig presets (drop `baseUrl`, move to `paths`/`bundler` resolution), verify nest/vite resolution, then un-ignore.                                                                                                                              |
| 12  | **Version Packages PR gets no CI**               | The release workflow opens the PR with `GITHUB_TOKEN`, and GitHub starts no workflows from that token's events.                                                                                                                                                           | A release PR could be merged without checks.                                                                                                                                                           | Close and reopen the PR (or push an empty commit) before merging — see `docs/RELEASING.md`. Switch to a GitHub App token if this becomes error-prone.                                                                                                          |
| 13  | **api image uses pnpm's legacy `deploy`**        | pnpm 10's default `deploy` requires injected workspace packages; the api Dockerfile uses `deploy --legacy` rather than switching the workspace to `inject-workspace-packages`.                                                                                            | The legacy mode may be removed in a future pnpm major.                                                                                                                                                 | Evaluate `injectWorkspacePackages: true` in `pnpm-workspace.yaml` (affects local linking) when upgrading pnpm.                                                                                                                                                 |
| 14  | **NestJS pinned to v11**                         | NestJS 12 is available, but `@nestjs/throttler` (the API's rate limiter) still caps its peer range at v11, and v12's ESM packages failed our tests (`Reflect.getOwnMetadata is not a function`, OpenAPI decorator specs). The `@nestjs/*` major is ignored in Dependabot. | Missing NestJS 12 features (Standard Schema validation, `@nestjs/observe`).                                                                                                                            | When throttler supports v12: migrate the whole `@nestjs/*` group, import `reflect-metadata` explicitly at entry points/test setup, and re-run the contract and e2e checks, then revisit the `class-validator`/Zod validation split (DECISIONS.md, 2026-09-16). |
| 15  | **No backups yet**                               | The production database lives in one Docker volume (`workhub-db-data`) with no scheduled backup or documented restore.                                                                                                                                                    | A disk failure, bad migration, or deleted volume loses all data.                                                                                                                                       | Add a backup standard (schedule, retention, off-host copy, tested restore) and a backup job — planned as step 7 of the pre-deploy PR plan.                                                                                                                     |
| 16  | **Border and input contrast unmeasured**         | `--border` and `--input` (`oklch(0.922 0 0)` on white; `oklch(1 0 0 / 10%)`/`15%` on `oklch(0.145 0 0)`) came from the shadcn/ui defaults and have never been measured against WCAG 1.4.11 (3:1 for control boundaries).                                                  | Input and control boundaries may fail AA in one or both themes — a merge requirement.                                                                                                                  | Measure both themes, retune the tokens, and add the automated token contrast check (BACKLOG.md, ACCESSIBILITY.md).                                                                                                                                             |
| 17  | **One database role for migrate and api**        | The compose files give the `migrate` one-shot and the `api` service the same `POSTGRES_USER`, which the Postgres image creates as a superuser owning the schema.                                                                                                          | A compromised API process could alter the schema or read beyond its tables; there is no least-privilege boundary inside the database.                                                                  | Create a runtime role with data-only privileges on the app schema for `api`, keep the owner role for `migrate` (BACKLOG.md, SECURITY_STANDARDS.md).                                                                                                            |

## Principles for managing debt

- Prefer paying debt down opportunistically while touching nearby code.
- Never add **undocumented** debt: if you take a shortcut, add a row here.
- Security- and data-integrity-related debt is prioritised above convenience.
